// ============how it works=================
// input:
  // ll n, r
// output:
  // nCr
//nCrがllの範囲に収まるときに使える
//計算量はO(nlog(n))
// ========================================


//=============big_com============================
ll big_com(ll n, ll r) {
  ll res = n;
  for (ll i = 1; i < r; ++i) {
    res = res * (N - i) / (i + 1);
  }
  return res;
}
//=================================================
