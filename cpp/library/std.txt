//cout精度
cout << fixed << setprecision(15) << y << endl;

//連続する要素を一つにする
X.erase(unique(X.begin(), X.end()), X.end());

//map for文
map<ll, ll> mp;
for (const auto& [key, value] : mp){
  std::cout << key << " => " << value << "\n";
}
