#include <bits/stdc++.h>
#define INF 2000000000000000000
#define ll long long
#define ld long double
#define pll pair<ll, ll>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  while (true) {
    ll N;
    cin >> N;
    if (N == 0) {
      break;
    }
    for (ll i = 0; i < N; ++i) {
      ll x, y, r;
      cin >> x >> y >> r;
    }
    cout << 1 << "\n";
  }

}
